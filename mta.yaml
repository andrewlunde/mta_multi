ID: mta_multi
_schema-version: '2.1'
description: Multi-Target Application for Multi-Tenant Use-Case
version: 0.0.1

modules:
 

 - name: web
   type: html5
   path: web
   parameters:
      app-name: multi-web
      memory: 512M
      disk-quota: 512M
      host: multi-web

